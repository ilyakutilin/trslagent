[input_data]
source_lang = "en"
target_lang = "ru"
# specialized_in = "technical and contractual translation"
# doc_type = "letter"
# doc_title = "Construction Constraints"
# additional_instructions = "Preserve all formatting and structure."
source_file_path = "files/source.txt"
# target_file_path = "files/target.txt"
# auto_glossary = true
# user_glossary_file_path = "files/glossary.txt"
# ref_source_file_path = "files/ref_source.txt"
# ref_target_file_path = "files/ref_target.txt"
# ref_max_length = 10000

[output_data]
# result_file_path = "files/result.md"
# raw_result_file_path = "files/raw_result.json"
# timestamped_result_filenames = true
# print_prompt_only = false

[llm]
# base_url = "https://openrouter.ai/api/v1"
# model = "qwen/qwen3.7-max"
# temperature = 0.1
# reasoning_effort = "high"  # none, minimal, low, medium, high, xhigh

[chunk]
# size = 6000
# divider = "-"    # split on lines of repeated chars (---------- etc.); overrides size
# max_concurrent = 3
# delay_seconds = 1.5

[glossary]
# xml_dir_path = "files/glossary"
# known_abbrs_file_path = "files/abbrs"

[log]
# level = "INFO"  # "DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"

[cost]
# generation_info_url = "https://openrouter.ai/api/v1/generation"  # URL for cost lookup; set to enable cost display
# cost_key = "total_cost"  # key in the generation info JSON response
# cost_currency = "USD"    # currency for cost display

[proxy]
# enabled = true              # master switch; false = direct connections, env proxy vars ignored
# protocol = "http"           # http, https, socks5, socks5h, socks4, socks4a; empty = use env vars (ALL_PROXY etc.)
# host = "127.0.0.1"
# port = 1080
# username = ""
# NOTE: proxy password is a secret — set it via PROXY__PASSWORD in .env only, never in TOML

[geoblock]
# Block requests from these countries (ISO 3166 alpha2 / alpha3 / numeric codes or full names).
# Unrecognized values fail config validation and exit with code 1. Empty list disables geoblocking.
# countries = ["RU", "BY"]
